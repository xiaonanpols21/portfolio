/*
const express = require('express');
const router = express.Router();;
const controller = require("../controllers/WorkController");

router.get("/work", controller.get);

module.exports = router;
*/